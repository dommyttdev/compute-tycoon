# ADR-0002: 不変なネットワークトポロジー

- 状態: 採用
- 日付: 2026-07-11

## 文脈

配線、IPアドレス、経路表は相互に依存する。部分更新された状態を他スレッドから参照すると
到達性判定が不安定になる。

## 決定

`NetworkTopology`を不変値として扱い、変更操作は検証済みの新しいTopologyを返す。
ゲームはロック内で参照を一括置換する。

## 結果

経路解決は一貫した状態を参照でき、保存用Snapshotも作りやすい。変更のたびに小さな
コレクションを複製するが、現在のゲーム規模では単純性を優先する。
